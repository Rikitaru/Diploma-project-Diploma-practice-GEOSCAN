struct NewStartPoint {
    float altitude;
    double latitude;
    double longitude;
    float rotation;
     NewStartPoint() : altitude(0.0f), latitude (0.0), longitude (0.0), rotation (0.0f)
     {
     }
};